---
title: 'Liquid Clusteringを動かして理解する: OPTIMIZE/VACUUMとの関係'
tags:
  - Databricks
  - deltalake
  - LiquidClustering
private: false
updated_at: '2026-03-23T17:36:32+09:00'
id: 0c66f5c0784860652b75
organization_url_name: databricks
slide: false
ignorePublish: false
posting_campaign_uuid: null
agreed_posting_campaign_term: false
---
# Liquid Clusteringで何が変わるのか

Databricksが全ての新規テーブルに推奨している[Liquid Clustering(LC)](https://docs.databricks.com/aws/ja/delta/clustering)ですが、有効にすればOPTIMIZEやVACUUMは不要になるのでしょうか? この記事では、ノートブックを動かしながらLCの動作を確認し、この疑問に答えます。

結論を先に言うと、**LCを有効にしても、OPTIMIZEとVACUUMは引き続き必要**です。ただし、その役割は従来とは変わっています。

**前提条件**
- Databricks Runtime 15.4 LTS以上
- Unity Catalog環境

# data skippingの仕組み

LCを理解するには、まずDelta Lakeの**data skipping**を知る必要があります。

Delta Lakeでは、各Parquetファイルがテーブルに追加されるとき、ファイル内の各カラムについてmin値(最小値)とmax値(最大値)がDelta Log(トランザクションログ)に記録されます。

クエリに`WHERE region = 'APAC'`というフィルタがある場合、Delta Lakeは実際にファイルを読む前にDelta Logに記録されたmin/maxを確認します。

- file_00のregion: min='APAC', max='NA' → APACを含む可能性あり → **読む**
- file_01のregion: min='EMEA', max='LATAM' → APACを含む可能性なし → **スキップ**

これがdata skippingです。フィルタ条件に合わないファイルを読み飛ばすことで、クエリが高速化されます。

## data skippingの効果はデータの並び方で決まる

data skippingの効果は、ファイル内のデータの並び方に依存します。

![Screenshot 2026-03-23 at 17.26.33.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/8035da4f-7d57-4eb7-9fdf-0ae2c3fa92e1.png)

**クラスタリングなし**(左): 各ファイルに4リージョンすべてが混在しているため、全ファイルのmin='APAC', max='NA'となり、どんなフィルタでも全ファイルを読む必要があります。

**クラスタリングあり**(右): 同じリージョンのデータが同じファイルに集約されるため、各ファイルのmin/max範囲が狭くなり、不要なファイルをスキップできるようになります。

つまり、**同じキー値のデータを同じファイルに集めることで、各ファイルのmin/max範囲が狭くなり、スキップできるファイルが増える**。これがクラスタリングの効果の本質です。

参考: [Data skipping](https://learn.microsoft.com/ja-jp/azure/databricks/delta/data-skipping)

# データの並び方を制御する3つの方法

Delta Lakeにはデータの並び方を制御する方法が3つあります。

| 観点 | パーティショニング | ZORDER | Liquid Clustering |
|---|---|---|---|
| 仕組み | ディレクトリ分割 | OPTIMIZE時に並べ替え | OPTIMIZE時に並べ替え |
| キーの変更 | テーブル再作成が必要 | OPTIMIZE時に都度指定 | `ALTER TABLE`で即変更 |
| キーの数 | 1〜2が限界 | 実質制限なし | 最大4 |
| 高カーディナリティ | 小さいファイルが大量発生 | 問題なし | 問題なし |
| OPTIMIZEの処理方式 | コンパクションのみ | 毎回全ファイル再書込 | **増分処理**(未処理分のみ) |
| VACUUMの要否 | 必要 | 必要 | 必要 |
| 併用 | ZORDERと併用可 | パーティションと併用可 | **どちらとも併用不可** |

LCはパーティショニングとZORDERの**置き換え**として設計されています。Databricksはすべての新規テーブルでLCの使用を推奨しています。

![Screenshot 2026-03-23 at 17.27.15.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/ada96740-fcaa-417f-8274-587221c39a74.png)

ZORDERは毎回全ファイルを再書き込みするのに対し、LCのOPTIMIZEは未処理のファイルだけを対象とする増分処理です。これがLCのOPTIMIZEがZORDERより軽い理由です。

参考: [Liquid Clustering](https://learn.microsoft.com/ja-jp/azure/databricks/delta/clustering) / [OPTIMIZE](https://learn.microsoft.com/ja-jp/azure/databricks/sql/language-manual/delta-optimize)

# ハンズオン: LCの動作を確認する

ここからはノートブックを使って実際にLCの動作を確認していきます。

## セットアップ

```python
CATALOG = "main"
SCHEMA = "liquid_clustering_lab"

spark.sql(f"USE CATALOG {CATALOG}")
spark.sql(f"CREATE SCHEMA IF NOT EXISTS {SCHEMA}")
spark.sql(f"USE SCHEMA {SCHEMA}")
```

## テーブルの作成

LCの効果を観察するために、**CLUSTER BYなし**でテーブルを作成してからデータを投入します。最初からCLUSTER BYを指定したテーブルに書き込むと、ファイルがある程度整理された状態で作られることがあり、OPTIMIZE前後の違いが見えにくくなるためです。

`delta.targetFileSize`を`256kb`に設定しているのは、ハンズオン用のデータ(約6MB)がデフォルトの1GB目標で1ファイルにまとまるのを防ぐためです。**本番環境ではデフォルトのまま使用してください。**

```sql
CREATE TABLE sales_lc (
  order_id BIGINT,
  order_date DATE,
  customer_id INT,
  region STRING,
  product STRING,
  amount DOUBLE,
  quantity INT
)
TBLPROPERTIES ('delta.targetFileSize' = '256kb');
```

## データ投入

実務でのデータ取り込みを再現するため、各ファイルに複数リージョンがランダムに混在するデータを生成します。

```python
from pyspark.sql import functions as F

regions_array = F.array(*[F.lit(r) for r in ["APAC", "EMEA", "NA", "LATAM"]])
products_array = F.array(*[F.lit(p) for p in ["Widget-A", "Widget-B", "Gadget-X", "Gadget-Y", "Service-Z"]])

df = (
    spark.range(0, 1_000_000, numPartitions=50)
    .withColumn("order_id", F.col("id"))
    .withColumn("order_date", F.date_add(F.lit("2023-01-01"), (F.rand(seed=10) * 730).cast("int")))
    .withColumn("customer_id", (F.rand(seed=42) * 10000).cast("int"))
    .withColumn("region", F.element_at(regions_array, (F.rand(seed=77) * 4).cast("int") + 1))
    .withColumn("product", F.element_at(products_array, (F.rand(seed=88) * 5).cast("int") + 1))
    .withColumn("amount", F.round(F.rand(seed=123) * 1000, 2))
    .withColumn("quantity", (F.rand(seed=456) * 20 + 1).cast("int"))
    .drop("id")
)

df.write.mode("append").saveAsTable("sales_lc")
```

## クラスタリング前の状態を記録

`snapshot`関数で現在のファイル状態を記録します。OPTIMIZE後と比較するためです。

```python
before_region = snapshot("sales_lc", "region")
```

```
記録完了: 50ファイル, 1ファイルあたり平均4.0リージョン
```


この時点では各ファイルに全リージョンが混在しているため、`WHERE region = 'APAC'`で検索しても全ファイルを読む必要があります。

# LCの有効化とOPTIMIZE

## ALTER TABLEでLCを有効化

```sql
ALTER TABLE sales_lc CLUSTER BY (region, order_date);
```

この操作は**メタデータの変更のみ**です。ファイルの物理的な再編成はこの時点では行われません。

## OPTIMIZEでクラスタリングを実行

LCテーブルでは`OPTIMIZE`だけでクラスタリングが行われます。`ZORDER BY`の指定は不要(指定するとエラー)です。

```sql
OPTIMIZE sales_lc
```

![Screenshot 2026-03-23 at 17.29.18.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/30530437-45e6-489e-97b6-4e83bcafab17.png)

## OPTIMIZEで何が変わったかを確認

`show_optimize_impact`でOPTIMIZE前後の変化をストーリーとして表示します。

```python
after_region = snapshot("sales_lc", "region")
show_optimize_impact(before_region, after_region)
```

```
======================================================================
 OPTIMIZEの効果 (region)
======================================================================

 [1. 再編成]
   OPTIMIZEが50個のファイルを読み込み、
   regionキーでデータを並べ替えて、48個の新ファイルに書き直した。

 [2. ファイルの中身の変化]
   OPTIMIZE前: 1ファイルあたり平均 4.0 種類のregion値を含んでいた
   OPTIMIZE後: 1ファイルあたり平均 1.0 種類のregion値を含む

 [3. クエリへの効果]
   WHERE region = ... で検索したとき、読むファイル数がどう変わったか:

   = 'APAC': 50/50ファイル → 12/48ファイル
     スキップ 0% → 75%  (38ファイル削減)
   = 'EMEA': 50/50ファイル → 12/48ファイル
     スキップ 0% → 75%  (38ファイル削減)
   = 'LATAM': 50/50ファイル → 12/48ファイル
     スキップ 0% → 75%  (38ファイル削減)
   = 'NA': 50/50ファイル → 12/48ファイル
     スキップ 0% → 75%  (38ファイル削減)

 [全体平均]
   読むファイル数: 50 → 12
   スキップ率: 0% → 75%
======================================================================
```

![newplot.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/2a4e1953-8661-4733-bed0-94b036a27cda.png)

![newplot (1).png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/8cbb5fe1-239c-4472-b676-189c7bc6db46.png)

OPTIMIZEがファイルを読み込み、regionキーでデータを並べ替えて新ファイルに書き直した結果、各ファイルのregion値が1〜2種類に絞られ、data skippingが効くようになりました。

order_dateでも同様の効果が確認できます。

```python
after_date = snapshot("sales_lc", "order_date")
show_optimize_impact(before_date, after_date)
```

```
======================================================================
 OPTIMIZEの効果 (order_date)
======================================================================

 [1. 再編成]
   OPTIMIZEが50個のファイルを読み込み、
   order_dateキーでデータを並べ替えて、48個の新ファイルに書き直した。

 [2. ファイルの中身の変化]
   OPTIMIZE前: 1ファイルあたり平均 730.0 種類のorder_date値を含んでいた
   OPTIMIZE後: 1ファイルあたり平均 60.8 種類のorder_date値を含む

 [3. クエリへの効果]
   WHERE order_date = ... で検索したとき、読むファイル数がどう変わったか:

   (730種類の値のうち代表的な値を表示)

   = '2023-01-01': 50/50ファイル → 4/48ファイル
     スキップ 0% → 92%  (46ファイル削減)
   = '2023-04-02': 50/50ファイル → 4/48ファイル
     スキップ 0% → 92%  (46ファイル削減)
   = '2023-07-02': 50/50ファイル → 4/48ファイル
     スキップ 0% → 92%  (46ファイル削減)
   = '2023-10-01': 50/50ファイル → 4/48ファイル
     スキップ 0% → 92%  (46ファイル削減)
   = '2023-12-31': 50/50ファイル → 4/48ファイル
     スキップ 0% → 92%  (46ファイル削減)
   = '2024-03-31': 50/50ファイル → 4/48ファイル
     スキップ 0% → 92%  (46ファイル削減)
   = '2024-06-30': 50/50ファイル → 4/48ファイル
     スキップ 0% → 92%  (46ファイル削減)
   = '2024-09-29': 50/50ファイル → 4/48ファイル
     スキップ 0% → 92%  (46ファイル削減)

 [全体平均]
   読むファイル数: 50 → 4
   スキップ率: 0% → 92%
======================================================================
```

## ファイルの増減を確認

`track_storage_changes`で各操作でのファイル増減を確認します。

```python
track_storage_changes("sales_lc")
```

![newplot (2).png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/a0d74683-ecf0-47d6-a5d6-0fe29c09c1d7.png)

OPTIMIZEの操作では青い棒(追加)とオレンジの棒(論理削除)の両方が出ています。旧ファイルを読み込んで新ファイルに書き直したため、追加と削除の両方が発生しています。

ここで重要なのは、オレンジの旧ファイルは**まだストレージに残っている**ということです。これを物理的に削除するのがVACUUMの役割です。

# VACUUMの役割

OPTIMIZEは:
1. 新しいファイルを**追加**する
2. 古いファイルをDelta Logから**論理削除**する
3. しかし古いファイルは**ストレージ上にまだ存在**している

古いファイルがすぐに消されない理由は、Delta LakeのTime Travel機能のためです。デフォルトでは**7日間**保持され、この期間を過ぎた古いファイルを物理的に削除するのがVACUUMです。

**VACUUMを定期的に実行しないと、OPTIMIZEのたびにストレージが膨張します。** つまり、LCを有効にしてもVACUUMは引き続き必要です。

```sql
-- テーブルの保持期間を0に変更(ハンズオン用。本番では絶対に使わないでください)
ALTER TABLE sales_lc SET TBLPROPERTIES ('delta.deletedFileRetentionDuration' = 'interval 0 hours');
VACUUM sales_lc;
-- 保持期間をデフォルトに戻す
ALTER TABLE sales_lc SET TBLPROPERTIES ('delta.deletedFileRetentionDuration' = 'interval 7 days');
```

VACUUM後の`track_storage_changes`で赤い棒(VACUUM物理削除)が出ていれば、旧ファイルがストレージから実際に消されたことを確認できます。

```python
track_storage_changes("sales_lc")
```

![newplot (3).png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/53361897-d747-41c2-9681-27d83a294044.png)

![Screenshot 2026-03-23 at 17.32.10.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/5ee3b76e-a75c-4f85-87c1-eb23c91dca6b.png)

これでWRITE → OPTIMIZE → VACUUMの一連のライフサイクルが完了しました。

参考: [VACUUM](https://learn.microsoft.com/ja-jp/azure/databricks/sql/language-manual/delta-vacuum)

# 補足トピック

## クラスタリングキーの変更

LCでは`ALTER TABLE`一発でキーを変更できます。パーティショニングではテーブルの再作成が必要でした。

```sql
ALTER TABLE sales_lc CLUSTER BY (region, order_date, customer_id);
```

この操作はメタデータの変更のみです。次にOPTIMIZEを実行すると、新しいキーに基づいてデータが再クラスタリングされます。全データを一度に再クラスタリングしたい場合は`OPTIMIZE テーブル名 FULL`を使います(DBR 16.0以降)。

参考: [クラスタリングキーの変更](https://learn.microsoft.com/ja-jp/azure/databricks/delta/clustering#change-clustering-keys)

## 自動Liquid Clustering(CLUSTER BY AUTO)

`CLUSTER BY AUTO`を使うと、Databricksがクエリの実行履歴を分析して最適なキーを自動的に選択・更新してくれます。Predictive Optimizationが有効な環境で利用可能です。

```sql
CREATE TABLE my_table (...) CLUSTER BY AUTO;
```

Predictive OptimizationはOPTIMIZE、VACUUM、ANALYZEの3つのメンテナンス操作も自動スケジュールします。つまり、CLUSTER BY AUTO + Predictive Optimizationで**キーの選択もメンテナンスもすべて自動化**されます。

参考: [自動Liquid Clustering](https://learn.microsoft.com/ja-jp/azure/databricks/delta/clustering#automatic-liquid-clustering) / [Predictive Optimization](https://learn.microsoft.com/ja-jp/azure/databricks/optimizations/predictive-optimization)

## 書き込み時の自動クラスタリング

LCテーブルでは、書き込みデータが一定の閾値を超えると書き込み時に自動でクラスタリングが行われます(clustering-on-write)。

閾値は`CLUSTER BY`で指定したキーの数によって変わります:

| CLUSTER BYの指定 | UC管理テーブルの閾値 | その他のDeltaテーブルの閾値 |
|---|---|---|
| `CLUSTER BY (col1)` — キー1つ | 64MB | 256MB |
| `CLUSTER BY (col1, col2)` — キー2つ | 256MB | 1GB |
| `CLUSTER BY (col1, col2, col3)` — キー3つ | 512MB | 2GB |
| `CLUSTER BY (col1, col2, col3, col4)` — キー4つ | 1GB | 4GB |

閾値以下の小さな書き込みでは発動しないため、**OPTIMIZEは引き続き必要**です。

参考: [書き込み時のクラスタリングのサイズ閾値](https://learn.microsoft.com/ja-jp/azure/databricks/delta/clustering#size-thresholds-for-clustering)

# まとめ

## LCでもOPTIMIZEとVACUUMは必要か? → はい、両方必要

**OPTIMIZE**
- LCテーブルではOPTIMIZEがクラスタリングのトリガーになる
- `CLUSTER BY`で設定したキーに基づいてデータが並べ替えられる(`ZORDER BY`の指定は不要)
- 既にクラスタリング済みのファイルはスキップされる(増分処理)ため、ZORDERより軽い

**VACUUM**
- OPTIMIZEがファイルを再編成すると、古いファイルが論理削除されてストレージに残る
- VACUUMを実行しないとストレージが膨張し続ける
- デフォルト保持期間は7日間

**Predictive Optimization**を使えば、OPTIMIZE、VACUUM、ANALYZEのすべてが自動スケジュールされます。`CLUSTER BY AUTO`と組み合わせるとキーの選択も自動化され、手動でのメンテナンスが不要になります。

## 運用ガイドライン

| シナリオ | OPTIMIZEの頻度 | VACUUMの頻度 |
|---|---|---|
| 頻繁なINSERT/UPDATEがあるテーブル | 1〜2時間ごと | 日次 |
| 日次バッチ処理のテーブル | バッチ後に1回 | 日次〜週次 |
| Predictive Optimizationが有効 | 自動(手動不要) | 自動(手動不要) |

## ノートブック

この記事で使用したノートブックは以下からダウンロードできます。

https://github.com/taka-yayoi/liquid_clustering_hands_on/

## 参考ドキュメント

- [テーブルにLiquid Clusteringを使用する](https://learn.microsoft.com/ja-jp/azure/databricks/delta/clustering)
- [Data skipping](https://learn.microsoft.com/ja-jp/azure/databricks/delta/data-skipping)
- [OPTIMIZE](https://learn.microsoft.com/ja-jp/azure/databricks/sql/language-manual/delta-optimize)
- [VACUUM](https://learn.microsoft.com/ja-jp/azure/databricks/sql/language-manual/delta-vacuum)
- [Predictive Optimization](https://learn.microsoft.com/ja-jp/azure/databricks/optimizations/predictive-optimization)

### はじめてのDatabricks

[はじめてのDatabricks](https://qiita.com/taka_yayoi/items/8dc72d083edb879a5e5d)

### Databricks無料トライアル

[Databricks無料トライアル](https://databricks.com/jp/try-databricks)
