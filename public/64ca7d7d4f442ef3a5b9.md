---
title: DatabricksノートブックにおけるSQLセルとPythonセルのデータのやり取り
tags:
  - Python
  - SQL
  - Databricks
private: false
updated_at: '2022-05-20T12:08:50+09:00'
id: 64ca7d7d4f442ef3a5b9
organization_url_name: databricks
slide: false
ignorePublish: false
---
本記事で説明しているノートブックはこちらです。

https://github.com/taka-yayoi/public_repo/blob/main/interaction%20between%20SQL%20and%20Python/Interaction%20between%20SQL%20and%20Python.py

SQLを用いてデータをロードし、Pythonを用いて結果を探索したいと思うかもしれません。Databricksノートブックの新機能としてSQLセルとPythonセルでネイティブにデータのやり取りが行えるようになりました。

DatabricksのPythonノートブックでは、SQL言語セルから得られるテーブルの結果は、自動的にPythonデータフレームとして利用できるようになります。Pythonデータフレームの名前は`_sqldf`となります。

https://qiita.com/taka_yayoi/items/dfb53f63aed2fbd344fc#%E3%83%8D%E3%82%A4%E3%83%86%E3%82%A3%E3%83%96%E3%81%ABpython%E3%82%92%E7%94%A8%E3%81%84%E3%81%A6sql%E3%82%BB%E3%83%AB%E3%81%AE%E7%B5%90%E6%9E%9C%E3%82%92%E5%8F%82%E7%85%A7

# データロード

Sparkデータフレームとしてデータをロードし、一時ビューとして登録します。

```py:Python
path = "/databricks-datasets/learning-spark-v2/flights/departuredelays.csv"

df = spark.read.option("inferSchema", True).option("header", True).csv(path)
df.createOrReplaceTempView("departure_delays")
```

# SQLからのアクセス

データは一時ビューとして登録されているのでSQLでアクセスできます。SQLで集計を行います。

```sql:SQL
%sql
SELECT origin, destination, count(*) as num_flights
FROM departure_delays
GROUP BY origin, destination
ORDER BY num_flights DESC
```

結果が以下のように表示されます。SQLの実行結果は`_sqldf`というSparkデータフレームに格納されます。

> SQL cell result stored as PySpark data frame `_sqldf`

![Screen Shot 2022-05-20 at 12.05.36.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/49b0d789-2f4a-342a-272c-ca725aaec7db.png)

# Pythonからのアクセス

Pythonセルからデータフレーム`_sqldf`にアクセスします。

```python:Python
display(_sqldf)
```

![Screen Shot 2022-05-20 at 12.06.25.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/b4f89bc5-93fe-caba-ea0a-b4b524be7e79.png)

pandasデータフレームに変換して、matplotlibを用いた可視化を行います。

```py:Python
pdf = _sqldf.toPandas()
```

```py:Python
import matplotlib.pyplot as plt

# 出発地がLAXの上位5件に限定
lax_pdf = pdf[pdf["origin"]=="LAX"][0:5]

x = lax_pdf['destination']
y = lax_pdf['num_flights']
label_x = lax_pdf['destination']

# 中央寄せで棒グラフ作成
plt.bar(x, y, align="center")           
plt.xticks(x, label_x)  # X軸のラベル
plt.show()
```
![Screen Shot 2022-05-20 at 12.07.13.png](https://qiita-image-store.s3.ap-northeast-1.amazonaws.com/0/1168882/1cc6df88-89e5-bac8-6729-c91791bf2ce4.png)

これまで以上に、SQLが得意とする処理はSQL、Pythonが得意とするPythonと棲み分けをしながらシームレスにロジックを記述できるようになりました！

### Databricks 無料トライアル

[Databricks 無料トライアル](https://databricks.com/jp/try-databricks)
