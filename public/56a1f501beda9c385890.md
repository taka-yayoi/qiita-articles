---
title: 'Auto Loader: ビルトインのデータ監査 & 検証を用いたデータの取り込み'
tags:
  - Databricks
private: false
updated_at: '2025-09-01T20:22:09+09:00'
id: 56a1f501beda9c385890
organization_url_name: databricks
slide: false
ignorePublish: false
posting_campaign_uuid: null
agreed_posting_campaign_term: false
---
[Auto Loader: Data Ingestion with Data Audit and Data Validation](https://community.databricks.com/t5/technical-blog/auto-loader-data-ingestion-with-built-in-data-audit-amp/ba-p/128587)の翻訳です。

:::note warn
本書は著者が手動で翻訳したものであり内容の正確性を保証するものではありません。正確な内容に関しては原文を参照ください。
:::

# イントロダクション: 頼りになる取り込みソリューションとしてのAuto Loader

Databricks Auto Loaderはクラウド環境におけるインクリメンタルデータ取り込みの標準となっています。これは、自動スキーマ推定を提供し、進化するデータ構造に対応し、一度のみの処理補償を確実なものにします。

Auto Loader機能の包括的な詳細については、[公式のDatabricks Auto Loaderのドキュメント](https://docs.databricks.com/aws/ja/ingestion/cloud-object-storage/auto-loader)をご覧ください。

Auto Loaderを素晴らしいものにしているのは、パイプラインの信頼性を維持し寿都もデータの完全性を保持する、洗練された破損レコードへの対応を含む、ビルトインのデータ品質、検証機能です。

# ユースケース: すべてのフォーマットにおける破損データの検知

現実世界のシナリオにおいては、破損データを含む場合のある複数フォーマットの様々な上流システムからファイルを取り込むことになります。この破損は以下のようなものがあります:

- パースできない不正な構造
- データ型のミスマッチ(数値フィールドのテキスト)
- 必要なフィールドや値の欠如
- 不正なフォーマットやエンコードの問題

従来のバッチ処理アプローチは多くの場合、破損したレコードによってファイル全体の処理を失敗するか、サイレントに破損したデータをスキップすることになります。

Auto Loaderは堅牢な破損データ検知によって、サポートされているファイルフォーマットにおいてこの課題を管理可能な特徴量に変換します。

# Auto Loader: ワンストップのファイル監査と検証ソリューション

Auto Loaderは、CSV, JSON, XML, Parquet、そのほかのサポートフォーマットに対する包括的なデータ検証機能を提供します:

- **自動スキーマ推定:** データ構造の変化に適応。
- **破損レコードの分離:** パイプラインを停止させることなしに不正レコードを分離。
- **ビルトインのレスキューモード:** `schemaEvolutionMode: "rescue"`で準拠しないデータを自動で捕捉。
- **パイプラインの再起動なし:** 取り込みを中断することなしに、スキーマ進化とデータ品質問題に対応。
- **ファイルレベルの追跡:** 正確な監査証跡のために破損レコードをソースまで追跡できる`_metadata.file_name`の活用。
- **柔軟な処理モード: コードを変更することなしにバッチ処理(availableNow=True)、リアルタイムストリーミング(processingTime)間をシームレスにスイッチ。
- **包括的なメタデータ:** 処理の統計情報やエラー詳細を追跡。
- **柔軟なハンドリングオプション:** 破損データに対応するための複数のアプローチ。

これによって、Auto Loaderはファイルフォーマット横断で一貫性を持って動作する取り込みツール、汎用的な監査ソリューションの両方として動作するようになります。詳細は: [Auto Loaderでのスキーマ推論と進化の構成](https://docs.databricks.com/aws/ja/ingestion/cloud-object-storage/auto-loader/schema)

# 二つのキーとなるオプションの理解

## 1. rescuedDataカラム

パース可能なポーションを抽出しつつも、準拠しない要素をrescuedDataカラムに保持することで、期待したスキーマに準拠しないデータを捕捉します。`cloudFiles.schemaEvolutionMode: "rescue"`を用いた際に自動で有効化されます - 追加の設定は不要です。すべてのサポートされるフォーマットのスキーマ推定とシームレスに動作します。詳細は、[Auto Loaderスキーマ進化はどのように機能しますか?](https://docs.databricks.com/aws/ja/ingestion/cloud-object-storage/auto-loader/schema#auto-loader%E3%82%B9%E3%82%AD%E3%83%BC%E3%83%9E%E9%80%B2%E5%8C%96%E3%81%AF%E3%81%A9%E3%81%AE%E3%82%88%E3%81%86%E3%81%AB%E6%A9%9F%E8%83%BD%E3%81%97%E3%81%BE%E3%81%99%E3%81%8B)をご覧ください。データリカバリーの最大化に理想的です。

## 2. _corrupt_recordカラム

パースできないレコードの全体を格納しつつも、他のカラムをnullにします。スキーマヒントを通じて明示的に有効化する必要があります。任意のファイルフォーマットにおけるデバッグや包括的な破損の追跡に最適です。

# PySparkを用いた実践的な例(CSVフォーカス)

:::note info
**注意** この分析はCSVフォーマットにフォーカスしています。Auto LoaderはCSV、JSON、XML、Parquet、そのほかのサポートフォーマットに対しても類似の破損データ検知機能を提供します。
:::

## シナリオの準備: 5つのキーとなるデータ検証シナリオ

```py
import time
from pyspark.sql.functions import col
from pyspark.sql.types import StructType, StructField, IntegerType, StringType

# Create unique path for experiment
path = f"/tmp/{int(time.time())}/"
input_path = path + "input/"
dbutils.fs.mkdirs(input_path)

# Expected schema for validation
schema = StructType([
    StructField("id", IntegerType(), True),
    StructField("product_name", StringType(), True),
    StructField("quantity", IntegerType(), True)
])

# Five data quality scenarios:

# 1. DataType Mismatch - text in numeric field (Detectable via Rescue)
dataType_mismatch = """id,product_name,quantity
101,Mechanical Keyboard,25
102,Ergonomic Chair,N/A"""

# 2. Missing Header - no column names (Detectable via Rescue) 
missing_header = """501,Webcam,20
502,USB-C Hub,15"""

# 3. Incorrect Delimiter - mixed delimiters (Detectable via Corrupt Record)
incorrect_delimiter = """id,product_name,quantity
301,Laptop Sleeve,40
302|Desk Mat|50"""

# 4. Additional Column - extra field (Detectable via Corrupt Record)
additional_column = """id,product_name,quantity
201,Mechanical Keyboard,25
202,Monitor Stand,30,Sold Out"""

# 5. Missing Column - incomplete record (Detectable via Corrupt Record)
missing_column = """id,product_name,quantity
401,Laptop Stand,35
402,Power Bank"""

# Write test files
files = {
    "dataType_mismatch.csv": dataType_mismatch,
    "additional_column.csv": additional_column, 
    "incorrect_delimiter.csv": incorrect_delimiter,
    "missing_column.csv": missing_column,
    "missing_header.csv": missing_header
}

for filename, content in files.items():
    dbutils.fs.put(f"{input_path}{filename}", content, overwrite=True)
    
print(f"Created {len(files)} test files covering key data validation scenarios")
```

## シナリオの準備: アウトプット

![](https://community.databricks.com/t5/image/serverpage/image-id/19119iC2E97076EA0B7FD2/image-size/large?v=v2&px=999)

## シナリオの準備: アウトプットファイルの中身の表示

```py
# Display the content of each file using dbutils.fs.head
print("--- Displaying File Contents ---")
print("DataType Mismatch:")
print(dbutils.fs.head(f"{input_path}dataType_mismatch.csv"))

print("Additional Column:")
print(dbutils.fs.head(f"{input_path}additional_column.csv"))

print("Incorrect Delimiter:")
print(dbutils.fs.head(f"{input_path}incorrect_delimiter.csv"))

print("Missing Column:")
print(dbutils.fs.head(f"{input_path}missing_column.csv"))

print("Missing Header:")
print(dbutils.fs.head(f"{input_path}missing_header.csv"))
```

![](https://community.databricks.com/t5/image/serverpage/image-id/19242iC19B9D4D86BF141D/image-dimensions/206x396?v=v2)

## シナリオ1: 既知のスキーマを用いたレスキューモード - ターゲッティングされた検知

定義済みのスキーマでレスキューモードを使用する際、Auto Loaderは期待される構造に準拠しないデータを検知しつつも、回復可能な情報を保持します。

```py
# Configure Auto Loader with known schema and rescue mode
(spark.readStream
  .format("cloudFiles")
  .option("cloudFiles.format", "csv")
  .option("header", "true")
  .option("cloudFiles.schemaEvolutionMode", "rescue")  # Enables automatic data rescue
  .schema(schema)  # Using predefined schema for validation
  .load(input_path)
  .select("*", "_metadata.*")
  .writeStream
  .format("delta")
  .option("checkpointLocation", path + "checkpoint_1/")
  .trigger(availableNow=True)
  .start(path + "output_1/")
  .awaitTermination()
)

# Analyze rescued data - detects DataType Mismatches and Missing Headers
df_rescued = spark.read.load(path + "output_1/")
rescued_records = df_rescued.filter(col("_rescued_data").isNotNull())

print(f"=== SCENARIO 1: RESCUE MODE WITH KNOWN SCHEMA ===")
print(f"Issues detected: DataType Mismatch, Missing Header scenarios")
print(f"Rescued records: {rescued_records.count()}")

display(rescued_records.select("_rescued_data", "file_name"))
```

## シナリオ1: アウトプット

![](https://community.databricks.com/t5/image/serverpage/image-id/19114i9EBD1C05DCAB109D/image-size/large?v=v2&px=999)

## シナリオ2: スキーマなしの破損レコード検知 - 構造上の問題

定義済みのスキーマなしに破損レコード検知を用いる際、Auto Loaderはスキーマ推定に依存し、まったくパースできないレコードを検知します。

```py
# Configure Auto Loader with corrupt record detection (no predefined schema)
(spark.readStream
  .format("cloudFiles")
  .option("cloudFiles.format", "csv")
  .option("header", "true")
  .option("cloudFiles.schemaLocation", path + "schema_2/")
  .option("cloudFiles.schemaHints", "_corrupt_record STRING")  # Enable corrupt record capture
  .load(input_path)
  .select("*", "_metadata.*")
  .writeStream
  .format("delta")
  .option("checkpointLocation", path + "checkpoint_2/")
  .trigger(availableNow=True)
  .start(path + "output_2/")
  .awaitTermination()
)

# Analyze corrupt records - detects Incorrect Delimiter, Additional Column, Missing Column
df_corrupt = spark.read.load(path + "output_2/")
corrupt_records = df_corrupt.filter(col("_corrupt_record").isNotNull())

print(f"=== SCENARIO 2: CORRUPT RECORD WITHOUT SCHEMA ===")
print(f"Issues detected: Incorrect Delimiter, Additional Column, Missing Column scenarios")
print(f"Corrupt records: {corrupt_records.count()}")

display(corrupt_records.select("_corrupt_record", "file_name"))
```

## シナリオ2: アウトプット

![](https://community.databricks.com/t5/image/serverpage/image-id/19115iCACB9A9D09B5DD67/image-size/large?v=v2&px=999)

## シナリオ3: 複合アプローチ - すべてのシナリオに対する堅牢な検知

レスキューモードと、既知のスキーマによる破損レコード検知を用いることで、すべてのデータ品質問題を包括的にカバーすることになります。

```py
# Enhanced schema including corrupt record field
schema_with_corrupt = StructType([
    StructField("id", IntegerType(), True),
    StructField("product_name", StringType(), True),
    StructField("quantity", IntegerType(), True),
    StructField("_corrupt_record", StringType(), True)
])

# Configure Auto Loader with both rescue mode and corrupt record detection
(spark.readStream
  .format("cloudFiles")
  .option("cloudFiles.format", "csv")
  .option("header", "true")
  .schema(schema_with_corrupt)  # Known schema with corrupt record field
  .option("cloudFiles.schemaEvolutionMode", "rescue")        # Auto rescue mode
  .option("cloudFiles.schemaHints", "_corrupt_record STRING") # Explicit corrupt detection
  .load(input_path)
  .select("*", "_metadata.*")
  .writeStream
  .format("delta")
  .option("checkpointLocation", path + "checkpoint_3/")
  .trigger(availableNow=True)
  .start(path + "output_3/")
  .awaitTermination()
)

# Comprehensive analysis - detects ALL five scenarios
df_combined = spark.read.load(path + "output_3/")
all_issues = df_combined.filter(col("_rescued_data").isNotNull() | col("_corrupt_record").isNotNull())

print(f"=== SCENARIO 3: ROBUST DETECTION WITH COMBINED APPROACH ===")
print(f"Total records processed: {df_combined.count()}")
print(f"Records with data quality issues: {all_issues.count()}")
print("Comprehensive detection: All 5 scenarios covered\n")

# Show detailed breakdown by issue type and source
print("--- Complete Data Quality Analysis ---")
display(all_issues.select("id", "product_name", "_rescued_data", "_corrupt_record", "file_name")
        .orderBy("file_name"))

# Detection summary
rescued_count = df_combined.filter(col("_rescued_data").isNotNull()).count()
corrupt_count = df_combined.filter(col("_corrupt_record").isNotNull()).count()
print(f"\n✓ DataType Mismatch & Missing Header → Rescued: {rescued_count}")
print(f"✓ Incorrect Delimiter, Additional & Missing Columns → Corrupt: {corrupt_count}")
print(f"✓ Total coverage: {((rescued_count + corrupt_count) / df_combined.count() * 100):.1f}%")
```

## シナリオ3: アウトプット

![](https://community.databricks.com/t5/image/serverpage/image-id/19120iC2D1F8DDCCFDA4AD/image-size/large?v=v2&px=999)

# 両方のアプローチを達成: 実装戦略

両方の検知戦略を活用するには:

1. 自動データリカバリーのために`cloudFiles.schemaEvolutionMode: "rescue"`で**デフォルトでレスキューモードを使用しましょう**
1. 必要な場合には、明示的に _corrupt_recordカラムを有効化するために、**破損レコードスキーマヒントを追加しましょう**
1. データ品質ダッシュボードで**両方のカラムを監視しましょう**
1. 許容できる閾値を破損率が超えた際の**アラートを設定しましょう**
1. それぞれの破損タイプに対応するために**下流の処理を実装しましょう**

# まとめ

Auto Loaderの破損レコードハンドリングによって、Auto Loaderは包括的なデータ品質ソリューションとなります。要件に応じて適切なアプローチを選択しましょう:

- データリカバリーを最大化するためのrescuedData
- 完全な破損追跡のための_corrupt_record
- 包括的なデータ品質モニタリングのためには両方

Auto Loaderによって、あなたの企業の要件に応じてスケールするデータの信頼の基礎を構築することになります。

### はじめてのDatabricks

[はじめてのDatabricks](https://qiita.com/taka_yayoi/items/8dc72d083edb879a5e5d)

### Databricks無料トライアル

[Databricks無料トライアル](https://databricks.com/jp/try-databricks)
